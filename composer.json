{
  "name": "yeswedev/auto-composer-update",
  "description": "A plugin that automatically updates the composer when a WordPress plugin is updated.",
  "type": "wordpress-plugin",
  "require": {
    "composer/installers": ">=2.0"
  }
}
